id: glinting-zones
name: 'Glinting zones'
priority: low
layer: integration
source_files:
  - src/engine/state.ts
  - src/engine/movement.ts
  - src/engine/renderer.ts
  - src/engine/effects.ts
  - src/engine/constants.ts
  - src/engine/types.ts
  - src/engine/gameLoop.ts
  - src/engine/glintZones.ts
  - src/components/InventoryGrid.tsx
dependencies:
  - player-movement
  - terrain-generation

behaviors:
  - id: patch-lifecycle
    description: >
      each glint patch has a lifecycle: fade-in (~30s), hold (~60s),
      fade-out (~30s), then removed. phase is computed from birth time.
      during fade-in and fade-out the sparkle density is scaled by the
      phase progress (0.0 to 1.0 during fade-in, 1.0 to 0.0 during
      fade-out). during hold the density is full. when fade-out
      completes the patch is removed from state.glintPatches.
    inputs:
      - 'tickGlintZones called each tick interval'
    outputs:
      - 'patches transition through phases and are removed when expired'
    state_changes:
      - field: glintPatches
        effect: 'expired patches removed each tick'
      - field: glintZones
        effect: 'rebuilt from active patches each tick'
    determinism: time-based

  - id: patch-drift
    description: >
      during the hold phase, each patch center drifts 1 tile in a
      random cardinal direction every GLINT_ZONE_DRIFT_MS (~10s).
      drift direction is picked randomly per step. the center is
      clamped to valid land tiles (Dirt or Clover). if a drift step
      would move onto non-land, the step is skipped. the tile set
      for the patch is recomputed after each drift. if the recomputed
      tile set would have fewer than GLINT_PATCH_MIN_TILES tiles
      (e.g. drifting toward a coastline erodes the patch), the drift
      step is skipped — the patch keeps its previous center and tile
      set, and tries again next drift tick. existing patches are not
      expired by drift erosion.
    inputs:
      - 'tickGlintZones called during hold phase'
    outputs:
      - 'patch center shifts by 1 tile, zone tile set recomputed'
    state_changes:
      - field: glintPatches
        effect: 'center position updated, lastDriftTime updated'
      - field: glintZones
        effect: 'rebuilt to reflect new patch positions'
    determinism: probabilistic

  - id: patch-spawning
    description: >
      tickGlintZones spawns a new patch every GLINT_ZONE_SPAWN_MS
      (~15s) if the active patch count is below GLINT_ZONE_COUNT (8).
      new patch center is randomly placed on Dirt or Clover within
      the land area (avoiding SPACE_BORDER). radius is random between
      GLINT_ZONE_RADIUS_MIN and GLINT_ZONE_RADIUS_MAX. spawnGlintPatch
      rejects any candidate whose computed tile set has fewer than
      GLINT_PATCH_MIN_TILES (5) valid land tiles — small slivers at
      coastlines or peninsulas are filtered out. up to 50 attempts per
      spawn; if all attempts fail, the spawn is skipped for that cycle.
      spawning is suppressed in cave.
    inputs:
      - 'tickGlintZones called when active patches < GLINT_ZONE_COUNT'
    outputs:
      - 'new GlintPatch added to state.glintPatches'
    state_changes:
      - field: glintPatches
        effect: 'new patch appended'
      - field: glintZones
        effect: 'rebuilt to include new patch tiles'
    determinism: probabilistic

  - id: initial-seeding
    description: >
      createGameState seeds GLINT_ZONE_COUNT initial patches with
      staggered birth times so they do not all fade in simultaneously.
      birth times are spread across the hold phase window so some
      patches are already holding and some are fading in at game start.
    inputs:
      - 'createGameState called'
    outputs:
      - 'state.glintPatches populated, state.glintZones derived'
    state_changes:
      - field: glintPatches
        effect: 'seeded with GLINT_ZONE_COUNT patches'
      - field: glintZones
        effect: 'derived from initial patches'
    determinism: probabilistic

  - id: coin-reglinting-on-walk
    description: >
      movePlayer in movement.ts checks if the new position is in
      state.glintZones when in the overworld. if so, all coins in
      the backpack that are not already glinting (uid not in
      glintingCoins) are restored to glinting status. for each coin
      that transitions from unglinted to glinted, its uid is recorded
      in state.coinGlintPopTimes with the current performance.now()
      timestamp — this is the signal the inventory UI uses to fire
      the pop animation. also records event:glint-zone discovery on
      first walk-over. coins already in glintingCoins are unchanged
      and do not get a new pop time.
    inputs:
      - 'player walks onto a glinting zone tile in overworld'
    outputs:
      - 'dull coins restored to glinting, pop times recorded, manual discovery recorded'
    state_changes:
      - field: glintingCoins
        effect: 'uids of all non-glinting backpack coins added'
      - field: coinGlintPopTimes
        effect: >
          for each coin that transitioned from unglinted to glinted in
          this call, its uid is set to performance.now() in the map.
          coins already glinted are not touched. coins not transitioned
          retain any prior pop time.
      - field: manualDiscoveries
        effect: 'event:glint-zone added on first walk-over'
    determinism: deterministic

  - id: inventory-coin-shimmer-and-pop
    description: >
      InventoryGrid renders each backpack coin in one of three visual
      states based on state.glintingCoins and state.coinGlintPopTimes.
      (1) Unglinted: coin background uses COIN_DULL_COLOR, no
      animation, no shimmer. (2) Glinted, no recent pop: coin
      background uses the definition's glyphColor and runs a sustained
      shimmer animation (loud, ongoing, visually distinct from
      unglinted). (3) Glinted with a recent pop (now - popTime <
      COIN_POP_DURATION_MS): coin runs both the pop animation (a brief
      scale-bounce + color flash, "big pop" per design) and the
      sustained shimmer. The pop is loud and clearly distinct from
      shimmer alone. The cell exposes data attributes
      (data-coin-state="dull" | "glint" | "glint-pop") so tests can
      assert the visual state without relying on CSS animation
      observation. The pop animation fires once per unglinted→glinted
      transition; coins that stay glinted between renders do not
      re-pop.
    inputs:
      - 'InventoryGrid rendered with a backpack containing coin items'
      - 'state.glintingCoins set, state.coinGlintPopTimes map, current time'
    outputs:
      - 'each coin cell carries a data-coin-state attribute matching its glint state'
    state_changes: []
    determinism: deterministic

  - id: pop-time-cleanup
    description: >
      state.coinGlintPopTimes is a transient signal. Entries older
      than COIN_POP_DURATION_MS are no longer meaningful but are not
      required to be removed for correctness — the inventory UI gates
      on (now - popTime < COIN_POP_DURATION_MS), so stale entries are
      visually inert. Entries for coins whose uid is no longer in the
      backpack (e.g. coin dropped or spent) are also visually inert
      because no cell renders them. No background cleanup tick is
      required.
    inputs:
      - 'time advances past COIN_POP_DURATION_MS after a pop'
    outputs:
      - 'inventory cell exits the pop animation, retains sustained shimmer'
    state_changes: []
    determinism: deterministic

  - id: sparkle-rendering
    description: >
      the renderer draws an animated sparkle overlay on tiles in
      state.glintZones. uses GLINT_ZONE_CHARS and GLINT_ZONE_COLORS.
      only a subset of tiles sparkle each frame. the density is
      modulated by the owning patch's phase opacity — during fade-in
      and fade-out, fewer tiles sparkle. the renderer looks up the
      patch opacity for each zone tile via a glintOpacity map
      (Map<string, number>) rebuilt alongside glintZones each tick.
      sparkles render only in the overworld; cave never shows sparkles.
    inputs:
      - 'state.glintZones set, state.glintOpacity map, current animation time'
      - 'current zone is Zone.Overworld'
    outputs:
      - 'sparkle glyphs rendered with phase-modulated density'
    state_changes: []
    determinism: time-based

  - id: beam-rendering
    description: >
      a deterministic ~30% subset of tiles in state.glintZones display
      a downward-slanting beam of '/' characters above them, suggesting
      light pouring through cracks in the clouds. each beam's length is
      a stable per-tile random integer between GLINT_BEAM_LENGTH_MIN
      and GLINT_BEAM_LENGTH_MAX (inclusive), and each beam picks a
      stable per-tile peak opacity in [0, GLINT_BEAM_MAX_OPACITY] so
      individual beams vary in brightness (some beams are nearly
      invisible). segments extend diagonally up-and-to-the-right from
      the glinting tile (segment i renders at world position
      (tileX + i + 1, tileY - i - 1) for i in 0..length-1). whether a
      given tile has a beam is decided by a stable per-tile hash so the
      same tile consistently has or lacks a beam. segments animate in
      a "pouring" pattern: the topmost segment (furthest from the
      ground) phases in first, then each lower segment phases in over
      the cycle, creating a top-down descent that loops continuously
      (cycle length GLINT_BEAM_CYCLE_MS). intensity also decays as the
      light pours: the topmost segment runs at the beam's peak opacity,
      and each successive segment toward the ground is dimmer, scaling
      linearly down to GLINT_BEAM_TAIL_OPACITY of the peak at the
      bottom segment. beam color is drawn from GLINT_ZONE_COLORS (gold).
      final per-segment alpha = patch phase opacity × pour-phase opacity
      × top-down falloff × beam peak opacity. beams render only in the
      overworld; cave never shows beams.
    inputs:
      - 'state.glintZones set, state.glintOpacity map, current animation time'
      - 'current zone is Zone.Overworld'
    outputs:
      - 'diagonal "/" beams rendered above ~30% of glinting tiles'
    state_changes: []
    determinism: time-based

  - id: glinting-cursor-effect
    description: >
      getTileEffects in effects.ts checks if the queried tile position
      is in state.glintZones (overworld only). if so, "glinting" is
      included in the returned effects array.
    inputs:
      - 'getTileEffects called with a tile position in state.glintZones'
      - 'state.currentZone is overworld'
    outputs:
      - 'effects array includes "glinting"'
    state_changes: []
    determinism: deterministic

edge_cases:
  - id: zone-on-sand
    description: 'patch center or drift lands near coastline'
    expected: 'only Dirt and Clover tiles within the radius are added; Sand and Space skipped'
  - id: overlapping-zones
    description: 'two patch circles overlap'
    expected: 'glintZones Set deduplicates; overlapping tiles appear once with the higher opacity'
  - id: max-attempts-exhausted
    description: 'all spawn attempts fail to find valid Dirt/Clover center'
    expected: 'fewer than target patches placed; no error'
  - id: cave-zone-suppression
    description: 'player is in the cave'
    expected: >
      glinting never happens in the cave: tick spawning is suppressed,
      sparkle rendering and beam rendering are skipped, the coin
      reglint check is gated on Zone.Overworld, and getTileEffects
      only reports "glinting" when in the overworld.
  - id: drift-to-edge
    description: 'patch drifts near map edge or coastline'
    expected: 'drift step skipped if center would land on non-land tile; patch stays in place'
  - id: all-patches-expire
    description: 'all patches fade out before new ones spawn'
    expected: 'glintZones becomes empty briefly; new patches spawn next tick cycle'
  - id: staggered-initial-lifecycle
    description: 'initial patches have spread-out birth times'
    expected: 'patches do not all fade in or expire at the same time'

  - id: patch-spawn-max-attempts-bounds
    description: 'Patch spawn attempts capped at 50; 0% chance of infinite loop'
    expected: 'If no valid center found after 50 attempts, spawn is skipped for that cycle'

  - id: patch-spawn-rejects-sub-min-size
    description: 'spawn attempt produces a candidate with fewer than GLINT_PATCH_MIN_TILES (5) valid land tiles (e.g. coastal sliver, peninsula tip)'
    expected: 'candidate is rejected; spawnGlintPatch tries another center within the 50-attempt budget. No patch with fewer than 5 tiles ever enters state.glintPatches.'

  - id: drift-erodes-below-threshold
    description: 'patch in hold phase tries to drift into a position whose computed tile set would have fewer than GLINT_PATCH_MIN_TILES tiles'
    expected: 'drift step is skipped; patch.centerX/centerY/tiles unchanged; lastDriftTime still updated so the patch tries again next drift tick. Patch is not expired by this.'

  - id: coin-picked-up-in-glint-zone
    description: 'player picks up a coin while standing on a glint zone tile, then the next movePlayer keeps them in (or re-enters) a glint zone'
    expected: 'newly-picked coin starts unglinted; on the next movePlayer that lands on a glint zone tile, the coin is added to glintingCoins AND its uid is recorded in coinGlintPopTimes — it pops like any other unglinted→glinted transition'

  - id: multiple-coins-pop-in-unison
    description: 'player walks onto a glint zone tile with several unglinted coins in the backpack'
    expected: 'all transitioning coins receive the same performance.now() timestamp in coinGlintPopTimes (single call site, single read of the clock), so their pop animations run in unison rather than staggered'

  - id: already-glinted-coins-do-not-repop
    description: 'player walks onto a glint zone tile while all backpack coins are already glinted'
    expected: 'glintingCoins is unchanged; coinGlintPopTimes is not mutated for those uids; no pop animation fires in the inventory'

  - id: pop-time-survives-inventory-open
    description: 'player picks up a coin in a glint zone (pop fires), then opens the inventory mid-pop'
    expected: 'inventory cell reads state.coinGlintPopTimes and runs the remaining portion of the pop animation; pop is not retriggered on inventory open because the transition condition is based on the engine-emitted timestamp, not on UI mount'

  - id: stale-pop-time-no-replay
    description: 'state.coinGlintPopTimes contains an entry from a transition more than COIN_POP_DURATION_MS ago'
    expected: 'cell renders as glinted with sustained shimmer only (data-coin-state="glint"); no pop animation. Stale entries are visually inert and need no active cleanup.'

  - id: beam-selection-deterministic
    description: 'whether a glinting tile has a beam, its length, and its peak opacity'
    expected: >
      presence (yes/no), length (in [GLINT_BEAM_LENGTH_MIN,
      GLINT_BEAM_LENGTH_MAX]), and peak opacity (in [0,
      GLINT_BEAM_MAX_OPACITY]) are all determined by stable per-tile
      hashes, so the same tile consistently has the same beam across
      frames; ~30% of glinting tiles have beams in aggregate, and beam
      peak opacities vary across the field.

  - id: beam-segments-off-screen
    description: 'glinting tile is near the top or right edge of the viewport'
    expected: >
      beam segments outside the viewport are skipped; on-screen segments
      render normally. no crash when segment world coords go out of bounds.

  - id: beam-over-non-land
    description: 'beam segments overlap sand, space, water, or other terrain'
    expected: >
      beam segments render as a visual overlay over any tile type; the
      underlying tile is not modified, and beam selection does not
      consider what is above the glinting tile.

  - id: beam-fade-with-patch
    description: 'patch is in fade-in or fade-out phase'
    expected: >
      beam segment opacity is multiplied by the patch phase opacity, so
      beams fade in and out together with their sparkles.

failure_conditions:
  - trigger: 'map has no Dirt or Clover tiles (all Space/Sand)'
    expected: 'all spawn attempts fail; glintPatches stays empty; no crash'
  - trigger: 'tickGlintZones called with no patches and spawn fails'
    expected: 'glintZones is empty Set; no sparkles rendered; no crash'
  - trigger: 'map terrain too fragmented for any candidate to reach GLINT_PATCH_MIN_TILES within 50 attempts'
    expected: 'spawn cycle is skipped; glintPatches stays at its current count; no error, no patch under 5 tiles'
  - trigger: 'InventoryGrid renders before state.coinGlintPopTimes is initialized (e.g. legacy save)'
    expected: 'missing map is treated as empty; all coins render in their glint/unglint state with no pop, no crash'

verification:
  test_file: src/engine/__tests__/glintZones.test.ts
  test_pattern: 'glint'
  command: npx vitest run src/engine/__tests__/glintZones.test.ts src/engine/__tests__/movement.test.ts src/components/__tests__/InventoryGrid.test.tsx
