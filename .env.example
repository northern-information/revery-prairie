# Origin of the deployed Cloudflare Worker. Empty in pure-offline builds.
# Local dev (wrangler dev): http://localhost:8787
# Production: https://revery-prairie-worker.<your-subdomain>.workers.dev
VITE_WORKER_URL=
