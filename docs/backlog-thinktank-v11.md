# Backlog thinktank — v11

A continuation of v10. v10 ran 2 rounds, locking the holographic day/night phase (four days and four nights per year, derived from `seasonalPhase`) and the price-it-don't-monetize-it doctrine for the artifact (one-time purchase, DRM-free, demo bounded to the first tenure). v11 opens with a question Tyler brought to the room on 2026-05-24: _do we need a concept of an equipped item now that meteorite hallowed ground (`RP-18`) and the field camera (`RP-23`) are in flight?_ The seed observation: the existing "[x] to drop" + drag-and-drop pipeline is vestigial from when the game was crafting-heavy, and the verb is wrong — you don't _drop_ a field camera, you _set it up_; you don't _drop_ a meteorite to make a sacred space, you _place_ it. The room found that v4 R7's _the inventory is the character sheet_ has been waiting for exactly this mechanic — the seam where carrying becomes meaning.

What follows is the rounds. Decisions are stated as decisions. Open questions are flagged. v11 is _additive_ to v10 — it does not invalidate v10 unless a round here explicitly amends it. Items that should propagate back to earlier versions are tagged `**Amendment to v{N}:**`.

## The cast

Carried forward from v2/v3/v4/v5/v6/v7/v9/v10 with no changes:

- **Astrid** — vision purist.
- **Boon** — systems-first.
- **Calla** — player-experience pragmatist.
- **Delta** — late-arriving frame-breaker.

## Open and locked

**Locked in round 1:**

- **Two verbs, not one.** `[x] to drop` is preserved untouched as a general inventory affordance for every item type. _Place_ is added as a second, distinct verb for placeable items. The steward chooses which gesture they mean.
- **New field on `GameState`: `equippedItemUid: ItemUid | null`.** Single-owner write through the inventory layer, registered in `EXPECTED_FIELDS`, uid-keyed for stack/sort/merge resilience like `state.glintingCoins` and `state.seedGenomes`.
- **Left-click (currently unused outside cursor positioning) becomes the place action when something is in hand.** Nothing-in-hand left-click behavior is preserved.
- **Drag stays for in-grid rearrangement.** Drag-from-grid-to-world is retired as the _placement_ path for placeable items; drop via `[x]` remains for those items.
- **Each placeable item registers a `PlaceableSpec`** — `{ verb, canPlace, place }`. Hover hints and in-game prose read the item's verb (_set_, _set up_, _lay_, _place_, _sow_), not a uniform substrate.
- **In-hand is sticky.** Each place consumes one; the stack decrements; in-hand auto-clears when empty. An explicit unequip path is required (right-click or a small keybinding, deferred to spec).
- **Loaded cursor.** The cursor's hot-pink highlight tile is unchanged in form. When something is in hand and the hovered tile passes `canPlace`, the cursor's inner glyph reads as the in-hand item. When `canPlace` is false, the loaded glyph drops out; the cursor returns to its plain highlight. Hot-pink-as-user-action stays locked.
- **Permacomputer HUD affordance.** A slot on the bottom-bar instrument cluster (`RP-28`) shows the in-hand item's glyph and name in hot pink. The bag holds; the hand declares.
- **The noun in player-facing copy is _in hand_, not _equipped_.** Code can keep `equippedItemUid` for engineering clarity; manual entries, hover hints, and prose read "in hand," "take in hand," "set down."
- **Amendment to v4 R7:** _the inventory is the character sheet_ now has its mechanic. The in-hand-and-place layer is the seam where that doctrine becomes a verb instead of a metaphor.

**Locked in round 2:**

- **The prairie speaks one visual language.** ASCII-on-canvas for the world, sparse UI affordances (dialog box, permacomputer, inventory grid) in their existing register, hot-pink reserved for steward-actions. No character has an animated portrait. Characters land through voice — dialog, music cue, glyph color.
- **Gron's `/gron.gif` is retired.** The asset is inheritance from a previous project, not a design choice. The `portrait?` field on `CharacterEntry` / `CharacterDefinition` is removed; `DialogBox` no longer renders portraits.
- **Emily is the threshold figure**, not Gron. Her authority is her stillness and her words ("you will return before the winter solstice, to revery"). The cosmology does not need a face for the first character.
- **`Gate` is flagged as not-a-character.** The locked-gate interactable wearing a `CharacterEntry` is the system telling us the `Character` type is overloaded. Deferred to its own round/spec — does not block the portrait retirement.

**Locked in round 3:**

- **Cliffs are not a tile type — elevation is the substrate, the threshold is the verb.** RP-41 is implemented by extending the existing `state.elevation` field (raise the genesis amplitude on `UpliftEpoch`) and gating movement on an `isClimbableStep(state, fromKey, toKey, threshold)` predicate that returns false when adjacent elevation deltas exceed the threshold. No new tile, no new glyph — the iso lift in `projection.ts` already does the 3D. **Amends the v7 round's framing of RP-41.**
- **The prairie does not owe the steward access.** Post-genesis, a connectivity pass from the spawn tile computes `state.reachableMass: Set<string>`. Tiles outside the mass remain part of the world — visible, lit, weathered, unwalkable. No ramp-carving to force full reachability. The unreachable cohort is the feature; mesas-you-can-see-but-not-touch are how the prairie expresses scale.
- **The cliff is a shadow, not a tile.** Renderer draws a dark cliff-face line on the iso side wall between adjacent tiles whose elevation delta exceeds the threshold — an extension to existing tile composition, not a new pass. One visual language (v11 R2) preserved.
- **X-ray, generalized.** The v7-round x-ray idea is recast as an alpha rule applied to any lifted tile whose iso projection would occlude the avatar's bounding box. No longer a cliff-tile property.
- **Amendment to v7 R3 (RP-41):** the framing of cliffs as an impassable tile type with stacked glyphs and arches is superseded. The implementation is elevation amplitude + walkable-step threshold + connectivity bookkeeping.

**Locked in round 4:**

- **The camera notices change, not events.** Capture is diff-driven — a single sim-loop hook at flora-lifecycle cadence appends a new `TimeLapseFrame` iff any of the 9 captured cells differs from the last frame on the roll. `recordCameraSubjectEvent`, its seven or eight site-specific call sites, the `CameraSubject` use in the camera pipeline, and the `subject` field on `TimeLapseFrame` retire. A baseline frame is captured at placement.
- **The photograph is the captured 3x3, iso-projected.** Playback renders the captured cells using the same iso transform `renderer.ts` uses for the world, at a smaller cell-pitch, framed with the existing `film-grain-overlay` class. The `GelBandView` exits primary playback and is preserved for the scan instrument.
- **The roll is a contact sheet.** Playback is a grid of all frames at once, chronological. Variance is itself the read — sparse roll says quiet patch, dense roll says lively one. Click-to-expand on a single frame is the held-read affordance; cross-fade lives in the expanded view only.
- **Film count = captured changes.** One detected change consumes one film unit, tightening the `RP-15` wear coupling.
- **The camera stays silent.** No instrument voice added to review.
- **Amendment to `RP-23` and `RP-24`.** Both inherit the new pipeline. Predecessor footage from `RP-24` uses a `degradation` prop (`{ grain, tint, glyphLeak }`) on the photograph component; current-tenure frames pass zero, predecessor frames pass non-zero. Contact-sheet density itself becomes a register the steward reads when encountering a predecessor's camera.

**Locked in round 5:**

- **The prairie has gravity now.** Waterfalls (`RP-64`) join wildfire (`RP-42`) and winter geology (`RP-44`) as the three motion-the-steward-cannot-direct systems. Cliffs gave the prairie _shape_; waterfalls give it _motion_. _Gravity was always older than fire._
- **Waterfalls are a system, not decoration.** Every waterfall must trace to a river or pond above it; standalone waterfalls are forbidden. Detection is a post-genesis pass over the union of `state.rivers ∪ state.ponds`, finding tiles with an unclimbable step (RP-41 `isClimbableStep`) to a lower walkable neighbor. New `state.waterfalls` field; render extends `tileBgCache.paintTileBg` on the same iso side wall as the cliff shadow.
- **The frozen waterfall is a stairway.** In winter, a frozen waterfall becomes climbable upward — `isClimbableStep` returns true for the bottom→top transition. Mesas above waterfalls become reachable in winter only; spring thaws the cascade and the route closes. _What is given in winter can be taken by the warmth that has no right to be there._
- **RP-43 thermogenic egregore counterpoint.** A frozen waterfall within an egregore's heat radius thaws locally during winter — the stairway dissolves. The egregore can revoke a route the prairie offered.
- **Amendment to v11 R3 (`reachableMass`).** The field is no longer purely genesis-time + RP-44 year-boundary. Frozen waterfalls extend `reachableMass` for the duration of winter; recompute hooks fire at season boundaries (Winter→Spring thaw, Autumn→Winter freeze) in addition to year-boundary geology.
- **Waterfalls are firebreaks.** Once RP-42 lands, fire-spread BFS rejects waterfall tiles for the same reason it rejects ordinary water tiles — moving water does not burn.

**Locked in round 6:**

- **The typewriter is not a system in v11.** No save-point semantics, no template messaging, no `state.pages`, no consumables. Tracked as `RP-66` so the room's reasoning persists.
- **The camera is the save point.** Shutter ≈ ribbon striking the platen — a slow, costly, deliberate act consuming a finite resource and producing an artifact that outlives the tenure. The prairie has its ritual instrument; a second one needs a real reason.

**Open in v11:**

- Does the cellar Knot eventually want a diegetic interface in the little house, and if so is the typewriter (`RP-66`) that interface?

---

## Round 1: to drop, and to place

_Tyler: "i think we might need a concept of 'equipped item' now. im working on meteorite hallowed ground and field camera. '[x] to drop' and drag and drop feels very unsatisfying. that mechanic/language is vestigial from when this was more of a crafting game with lots of inventory management. and truthfully you don't 'drop' a field camera. you set it up. and you don't 'drop' a meteorite to make a sacred space. you place it. gaming it out a bit, i think if you had a bunch of meteorites in your inventory you could equip any one of them and then probably left click (currently unused) would place them one at a time. you would need to equip each one individually."_

> **Astrid:** The verb is the relationship. To _drop_ a stone is to disown it — to let go of something you no longer want. To _place_ a stone is to dedicate it — to declare a position the stone is now to hold. The prairie's whole substrate has wanted the second verb for a long time; the crafting-game language is vestigial in exactly the way Tyler says, presuming the inventory was a buffer and the world was the destination for surplus. The prairie's inventory is not a buffer; it is what the steward is carrying, and the carrying matters. _Dropping_ is allowed — the steward should be free to disown a thing, the bag should not be a cage — but it must not be the _only_ verb. v4 R7 named the inventory the character sheet; this round is when that line earns a second verb. Each placeable item declares the sentence the steward is finishing — the meteorite is _set_ or _laid_; the camera is _set up_; a seed, when this lands on it, is _sown_. The drop is still there for the steward who just wants to put a thing down. The place is there for the steward who wants to _mean_ something by putting it down. _One verb is a warehouse. Two verbs is a relationship._

> **Boon:** Substrate. New field on `GameState`: `equippedItemUid: ItemUid | null`. Single-owner write through the inventory layer, registered in `EXPECTED_FIELDS`. uid-keyed for the same reason `state.glintingCoins` and `state.seedGenomes` are — survives `autoSort`, merge, stack, and split without losing the reference. Left-click, currently routed only to cursor positioning, becomes the place action when something is in hand; otherwise left-click stays cursor-only. The `[x] to drop` keymap is preserved untouched — drop remains a general inventory affordance for every item type, placeables included. What goes away is drag-from-grid-to-world _as the placement path for placeables_; drag stays for in-grid rearrangement, and drop-via-`[x]` stays for everything. Per-item placement is delegated to a `PlaceableSpec` registered alongside the item def: `{ verb: 'set' | 'set up' | 'sow' | 'lay' | 'place'; canPlace(state, x, y): boolean; place(state, x, y, uid): void }`. Meteorite spec appends to `state.placedMeteorites` and decrements the equipped stack; camera spec instantiates a `PlacedCamera` and consumes the slot entirely. HUD affordance: a slot on the permacomputer bottom-bar instrument cluster (`RP-28`) shows the in-hand item's glyph in hot pink with its name. The cursor's hot-pink highlight tile is unchanged in form; when something is in hand and the hovered tile passes `canPlace`, the cursor's inverted-dark foreground glyph swaps to the in-hand item's glyph — a "loaded" cursor reading as the steward's hand pre-empting the tile. When `canPlace` is false, the loaded glyph is suppressed (the cursor reads as its usual highlight, no payload), so legality is readable at a glance. Stack semantics: in-hand is sticky, each place consumes one, the stack decrements, in-hand auto-clears when empty. Right-click (or a keybinding deferred to spec) unequips. Net cost: one field, one registry, one input branch, one bottom-bar slot, a label sweep. Small-to-medium.

> **Calla:** The drag was twitchy and the verb was wrong; this fixes both at once without making the steward give anything up. Drop stays — there are weeks when the inventory is messy and the steward just needs to put a thing down — and place arrives as a distinct gesture for the moments that matter. Two verbs is more truthful than one. The HUD affordance is what makes the second verb legible: a slot on the permacomputer bottom bar shows what is in hand right now, glyph and name, in the same hot pink the world preview uses. The steward glances down and sees what they are _carrying with intent_, separate from what is in the bag. _That_ is what the drag-and-drop tried to express and could not. The bag holds; the hand declares — and the cursor confirms. The cursor's form stays the hot-pink tile the steward already recognizes; the only change is that the tile's inner glyph becomes the in-hand item when the steward is hovering a legal placement. A small, quiet affordance — not a new cursor mode, just a hint that the next click will mean something. When the hovered tile is illegal, the loaded glyph drops out; the cursor returns to its plain highlight, and the steward learns the boundary without a message. Concerns the spec needs to honor: an explicit unequip path so a hair-trigger left-click on a long walk does not surprise the steward; `canPlace` honest about which tiles are legal; per-item hover-hint copy that reads from the verb — _set down_, _set up_, _sow_ — and not from a uniform substrate.

> **Delta:** The frame to break is the word "equip." It is the right mechanic and the wrong noun — it carries an RPG inheritance the prairie is otherwise free of (no class, no level, no slot tree). The prairie's word is _in hand_. The steward has the meteorite _in hand_; the steward _takes the camera in hand_; the manual reads "to set down what is in hand, click." Small change, just labels — but labels are what the steward sees, and the prairie's language is half the game. The deeper frame, and this is the line worth locking, is that this round is the moment the inventory stops being a container and becomes a relationship. The grid is not a backpack; the grid is everything the steward is currently carrying _and currently meaning_. The bottom-bar slot is the second sentence of the same paragraph — the bag holds; the hand declares. Drop did not need to die for place to be born; the two verbs need each other. One open thing the round should name and not solve: when this lands, `RP-11`'s drop-on-dirt-to-plant becomes _sow-from-in-hand_, and the seed-into-soil contact gets the same treatment. That is not for this spec; that is the second-order tax this idea pays the rest of the inventory. _Two verbs is what makes one of them a ceremony._

### Consensus

- New field on `GameState`: `equippedItemUid: ItemUid | null`. Single-owner write through the inventory layer, registered in `EXPECTED_FIELDS`, uid-keyed for stack/sort/merge resilience.
- Left-click (currently unused outside cursor positioning) becomes the place action when something is in hand. Nothing-in-hand left-click behavior is preserved.
- `[x] to drop` is preserved untouched. Drop remains a general inventory affordance for every item type, placeables included. Place is _additive_, not a replacement — the steward has two verbs available.
- Drag stays for in-grid rearrangement. Drag-from-grid-to-world is retired as the _placement_ path for placeable items; drop via `[x]` remains for those items.
- Each placeable item registers a `PlaceableSpec` — `{ verb, canPlace, place }`. Hover hints and in-game prose read the item's verb.
- In-hand is sticky: each place consumes one; the stack decrements; in-hand auto-clears when empty. An explicit unequip path is required (right-click or a small keybinding, deferred to spec).
- The cursor's hot-pink highlight tile is unchanged in form. When something is in hand and the hovered tile passes `canPlace`, the cursor's inner glyph reads as the in-hand item — a "loaded" cursor. When `canPlace` is false, the loaded glyph drops out; the cursor returns to its plain highlight. Hot-pink-as-user-action stays locked.
- Permacomputer HUD affordance: a slot on the bottom-bar instrument cluster (`RP-28`) shows the in-hand item's glyph and name in hot pink. The bag holds; the hand declares.
- The noun in player-facing copy is _in hand_, not _equipped_. Code can keep `equippedItemUid` for engineering clarity; manual entries, hover hints, and prose read "in hand," "take in hand," "set down."
- **Amendment to v4 R7:** _the inventory is the character sheet_ now has its mechanic. The in-hand-and-place layer is the seam where that doctrine becomes a verb instead of a metaphor.

### Tracked as

- **`RP-59 In-hand + place (additive placement verb, HUD affordance)`** — M, depends on `['RP-18', 'RP-23', 'RP-28']`. New `equippedItemUid` field, `PlaceableSpec` registry, left-click placement routing, loaded-cursor glyph swap, unequip path, permacomputer bottom-bar in-hand slot. `[x] to drop` left intact for all items.
- **Amendment to `RP-18`:** meteorites are placed via in-hand + left-click. They can still be dropped via `[x]` (as a regular `ItemDrop`); place vs. drop are now two distinct verbs for the same item, with different semantics (drop → ground item; place → `state.placedMeteorites` and hallowed-ground geometry).
- **Amendment to `RP-23`:** the field camera is _set up_ from in-hand. The camera's `place` consumes the inventory slot entirely (unique artifact). Drop via `[x]` remains available.

### Open questions deferred to specs

- Does `RP-11` (seeds, drop-on-dirt-to-plant) migrate to in-hand-and-sow on the same pass, or follow-up? (Delta, Boon) Room's lean: follow-up; ship `RP-59` for meteorites and camera first.
- The exact unequip input — right-click vs. `[e]` vs. clicking the in-hand slot. (Calla, Boon) Defer to spec.
- The exact location of the HUD affordance within the bottom-bar instrument cluster (`RP-28`) — left of the ambient instruments? Right? Center? (Calla)
- The exact treatment of a "loaded" cursor over an illegal tile — drop the glyph entirely (lean), or render it muted? (Boon, Calla) Defer to spec.
- Whether non-placeable items can also be _in hand_ as a future hook (e.g. for `RP-15` wear-on-use). (Boon) Lean: placeable-only for now.
- Per-item hover-hint copy from `PlaceableSpec.verb` — editorial pass before lock. (Astrid)

---

## Round 2: the gif from the previous apartment

_Tyler: "what do you think about adding character animations for npcs? right now we only have gron. i like it. it is evocative. but it is the only non-ascii or canvas asset. i'm not particularly excited to make them, oddly. i'm wondering if they help or hurt the game from a cosmological sense. i think it gives characters more weight, but it feels sorta odd to then have nothing similar for the prairie which is the real main character. (after a beat: gron is not the first character — emily is. and i really don't know if we should keep gron. it's kinda all or nothing to me. he was a previous asset i had made years ago and just slotted in.)"_

> **Astrid:** Then the cosmology has already answered. Emily is the threshold — the girl who waits inside the little house, the voice that delivers the canonical refrain, _you will return before the winter solstice, to revery_. Emily has no portrait. She does not need one. Her authority is in her words and her stillness; the steward meets her in ASCII and the line lands harder for it. If Emily can carry that weight without a face, every other character in the cast can. Gron's gif is not the threshold's portrait, because the threshold is already accounted for. It is, as Tyler said plainly, an asset from a previous game that got slotted in. _Listen to what the hand will not make._ Tyler's lack of excitement to make more is not creative block — it is the cosmology speaking through the hand, and the hand is saying the prairie does not want faces. The prairie is the main character; the steward is the only body that gets to move; the named ones speak and leave. _A face would round the steward's interlocutors into people. The cosmology asks them to remain visitors._ Remove the gif. The prairie does not have a portrait, and neither should anyone in it.

> **Boon:** Field audit. `src/engine/characters.ts:11` declares `portrait?: string`. Two characters set it: `gron` at line 24 and `coyote` at line 54 (both pointing at `/gron.gif` — Coyote's is a copy-paste). Every other character — Moab, Gate, Emily, the ghost cohort — leaves it unset. The optional field is one-use, and the one use is the asset Tyler is questioning. Cleanup path is small: delete `/gron.gif` from `public/`, remove the `portrait` field from `CharacterEntry` and `CharacterDefinition`, drop the `portrait?` prop from `DialogBox` and its test fixtures (`src/components/__tests__/DialogBox.test.tsx:80-82`), and let the dialog box render uniformly across the cast. Net: one asset retired, one optional field retired, one prop and its alt-text test retired. Small. While the hand is in there: `gate` at line 57-62 is not a character. Glyph `#`, one line, no music, no gift — it is a locked-gate interactable wearing a `CharacterEntry`'s clothes because the dialog pipe was the easiest route. That is a separate concern; flag it as a follow-up, do not bundle. The portrait removal does not depend on resolving it. _The cheapest spec is the one that retires a field instead of populating it._

> **Calla:** The word Tyler used was "evocative" — and that is real. Gron's gif _is_ evocative. The question is what is doing the evoking: the animation itself, or the novelty of a moving face in an otherwise still-and-rendered world? The answer is almost certainly the novelty. Extend it to four NPCs and the gif stops feeling like a window into someone and starts feeling like a portrait gallery — and a portrait gallery is exactly the kind of RPG inheritance the prairie has been avoiding. Strip it back to none and the dialog box becomes a different shape of intimacy: the steward and a voice, in the same visual register as the prairie itself. Emily's line, in particular, lands harder without a face attached to it; the absence is the weight. _One register for everyone, including the threshold._

> **Delta:** The frame to break is "should we add animations." Wrong question, and Tyler already broke it himself when he said _all or nothing_. The actual decision is whether to keep a piece of furniture from the apartment you moved out of. Gron's gif is doing two things at once — it looks like an art-direction choice, and it is actually inheritance from a previous project. Inheritance and intention are different things. The prairie has built a coherent visual language: ASCII-on-canvas for the world, sparse UI affordances for the bag and the dialog and the permacomputer, hot-pink reserved for steward-actions. The gif is the one piece that does not speak that language. _The camera does not know it is the camera, and the prairie does not know it is the prairie_ — but the gif knows it came from somewhere else. Retire it. While you are at it: `Gate` is a clue. The fact that a locked door is implemented as a `Character` is the system telling you the `Character` type is overloaded — it currently means "anything you can press `[f]` on and get a line back from." That is a different round, but worth naming so it does not get lost. _One visual language, no inheritance, no faces._

### Consensus

- **Remove Gron's portrait.** Delete `/gron.gif` from `public/`, remove the `portrait?` field from `CharacterEntry` (`src/engine/characters.ts:11`), `CharacterDefinition` (`src/engine/types.ts`), and the `Character` runtime type. Drop the `portrait?` prop from `DialogBox` and update the test fixture (`src/components/__tests__/DialogBox.test.tsx:80-82`).
- **Coyote's `/gron.gif` reference dies with the field** — no separate fix needed.
- **The prairie speaks one visual language**: ASCII-on-canvas for the world, sparse UI affordances (dialog box, permacomputer, inventory grid) in their existing register, hot-pink reserved for steward-actions. No character has an animated portrait. Characters land through voice — their dialog, their music cue, their glyph color.
- **Emily is the threshold figure** by virtue of being the first character the steward meets and the carrier of the canonical refrain. Her authority is in her stillness and her words. No portrait needed; no portrait wanted.
- A one-line conventions note in `CLAUDE.md` confirming the no-portrait doctrine, so future contributors (and future-Tyler) do not read absence as a TODO.

### Tracked as

- **`RP-61 Retire Gron portrait and the portrait field`** — S, no deps. Delete `/gron.gif`. Remove `portrait?: string` from `CharacterEntry` in `src/engine/characters.ts:11`, from `CharacterDefinition` in `src/engine/types.ts`, and from the `DialogBox` props and its test (`src/components/__tests__/DialogBox.test.tsx`). Add a one-line conventions note in `CLAUDE.md` declaring that no character has an animated portrait — the prairie speaks one visual language.
- **`RP-63 Gate is not a character`** — XS, no deps, deferred. The locked-gate interactable at `src/engine/characters.ts:57-62` is routed through the `Character` system because `[f]`-to-talk was the easiest path. Spec out whether `Character` should be split into `Character` (named persons with dialog) and `Interactable` (things that respond to `[f]` with a line), or whether Gate should move to a different system entirely. Not blocking on `RP-61`.

### Open questions deferred to specs

- _(none — round produced a lock, not new uncertainty.)_

---

## Round 3: elevation has an opinion now

_Tyler: "i think this feature is a matter of increasing the height possible during genesis and introducing a 'walkable' threshold level. the rub is going to be balancing it out so that either all tiles are topographically accessible OR accepting that some mesas, for example, might not be accessible."_

> **Astrid:** Then the question is older than the spec. The prairie has always had places the eye can hold that the feet cannot — distant escarpments in the Loess Hills, the bluffs above the Mississippi, an Ozark glade catching late-afternoon light across a draw you cannot cross before dark. That is not a defect of the landscape; that is the landscape having _shape_. The cosmology's answer to Tyler's rub is direct: _the prairie does not owe the steward access._ A mesa visible but unreachable is the prairie expressing its own scale, the same way the year is unreachable in compressed form. Force-carving ramps until every tile is connected would be the same impulse as the boot animation Round 7 of v4 banned — the consumer-electronics gesture of guaranteeing the steward gets what they see. Refuse it. Some places are seen and not walked. _What you cannot reach, you keep more carefully._ The threshold itself becomes a register the steward learns: a single step up is a stride, two is a scramble the boots refuse. The cosmology has held thresholds before; this is one more.

> **Boon:** Substrate is already in the tree. `state.elevation: Map<string, number>` exists today (`src/engine/genesis/state.ts:55`, hoisted onto `GameState` at `src/engine/state.ts:311`), is uplifted during genesis on a 0–100 scale, and is read cosmetically by `getElevationLift` (`src/engine/projection.ts:210`) and weighted into the lightning weight (`src/engine/lightning.ts:128-129`). It is not yet read by `movement.ts`. The work, in order: (1) turn up the genesis amplitude on `UpliftEpoch` so the existing field starts producing structurally meaningful peaks instead of cosmetic bumps; (2) add an `isClimbableStep(state, fromKey, toKey, threshold)` predicate that returns false when `|elev(to) - elev(from)| > threshold`; (3) gate `src/engine/movement.ts:106` on _both_ `isWalkableTile` _and_ `isClimbableStep`; (4) run a post-genesis connectivity pass from the spawn tile, mark the resulting set as the _reachable mass_, leave the rest as the unreachable cohort — no carving, no edits, just bookkeeping; (5) `state.reachableMass: Set<string>` registered in `EXPECTED_FIELDS`, used by ruin placement, character spawning, and anything else that wants to guarantee its target sits in the reachable cohort. Render side: no new tile type, no new glyph, no new pass — the iso lift is already doing the 3D for free, and v11 R2 locked one visual language. The v7-round x-ray idea becomes an alpha rule in `tileBgComposite.ts`: if a tile's lift would occlude the avatar's bounding box, drop its alpha. That is a small render change, not a new pass. The cost of this whole spec is one new field, one new predicate, one connectivity pass, one branch in movement, and an alpha rule. Small-to-medium. The expensive part is _tuning_ the amplitude and threshold so the prairie produces shapes the eye reads as bluffs and mesas instead of noisy hills.

> **Calla:** When the steward walks toward a step they cannot take, they need to _know_ before they collide with it — otherwise the threshold reads as random ("I climbed that one, why not this one?"). The lift already gives a visual cue, but the system needs a second one: a dark cliff-face line drawn on the iso side wall between two tiles whose delta exceeds the threshold. That is the cliff. Not a tile, not a glyph — a shadow on the side of the rise. The renderer already draws lifted tiles; this is the line at their base where the lift becomes uncrossable. Click-to-move pathfinding has to honor `isClimbableStep` or the steward will queue a path that strands them halfway up a bluff. And the "minor procedural cliffs" of the v7 round need a craft check: a three-tile pimple sticking up in the middle of an otherwise walkable field is the frustrating shape; a long ridge the steward can walk _along_ until they find the saddle that lets them cross is the satisfying one. The amplitude should produce ridges and mesas, not warts. Astrid's _seen and not walked_ is a beautiful sentence when the unreachable thing is the distant bluff with the lone tree on top; it is just annoying when it is a 2x2 island in a clearing. Tune the noise toward _landmark scale_.

> **Delta:** The frame to break is the word "cliffs." The room has been talking about cliffs since v7, and Tyler just renamed the feature without saying it. There are no cliffs. There is _elevation that finally matters_. The cliff is what the steward calls the place where the elevation field has an opinion strong enough that the feet have to listen. Same field that has been sitting in `state.elevation` all along — same lift, same lightning weighting — just turned up far enough that movement notices. This is the round to lock the framing: **elevation is the substrate, the threshold is the verb, the cliff is the noun the steward applies when they bump into it.** Rename in the manual: not "cliff," but "rise" or "scarp" or "the bluff" — whatever the species of landform is. And lock the second thing the cosmology has already answered: _the reachable mass is not the whole map._ A spawn-connected reachable cohort, an unreachable cohort, no carving. The unreachable cohort is not a bug; it is what makes the reachable cohort feel _chosen_. _Permanence is a capitalist assumption_ was v4's line; this round's is **the prairie does not owe the steward access**. Tyler's rub dissolves: he was asking which of the two options is the right one, and the right one is the one the cosmology has been waiting for the substrate to enable. Accept the inaccessibility. The inaccessibility is the feature.

### Consensus

- **No new tile type, no new glyph.** RP-41 is implemented by extending the existing `state.elevation: Map<string, number>` field — turn up the genesis amplitude on `UpliftEpoch` so peaks are structurally meaningful instead of cosmetic — and gating movement on a per-step elevation delta threshold. **Amends the v7 round's framing of RP-41**, which treated cliffs as a new impassable tile type.
- **Walkability rule.** Add `isClimbableStep(state, fromKey, toKey, threshold)` returning false when `|elev(to) - elev(from)| > threshold`. Gate `src/engine/movement.ts:106` on both `isWalkableTile` _and_ `isClimbableStep`. Click-to-move pathfinding honors the same predicate.
- **Reachability is not guaranteed.** Post-genesis, walk a connectivity pass from the spawn tile to compute `state.reachableMass: Set<string>`. No carving, no ramp-stamping — tiles outside the mass remain part of the prairie, visible from afar, unwalkable. _The prairie does not owe the steward access._ Ruin placement, character spawning, and any other system that requires the steward be able to reach its target reads `reachableMass` as a precondition.
- **New `GameState` field: `reachableMass: Set<string>`.** Single-owner write at genesis, registered in `EXPECTED_FIELDS`. Recomputed if RP-44 winter geology lifts/drops elevation.
- **The cliff is a shadow, not a tile.** Renderer adds a dark cliff-face line drawn on the iso side wall between adjacent tiles whose elevation delta exceeds the threshold. No new pass — extension to `tileBgComposite.ts` or its sibling, consistent with the one-visual-language lock from v11 R2.
- **X-ray, revised.** The v7 round's x-ray idea survives but is no longer a cliff-tile property. It becomes an alpha rule in the render side: any tile whose iso lift would occlude the avatar's bounding box drops its alpha. Applies to all lifted tiles, not just cliffs.
- **Amplitude tuning targets _landmark scale_.** The noise should produce ridges the steward can walk along until they find a saddle, and mesas large enough that "I can see it but cannot reach it" reads as scale, not as a 2x2 wart in a clearing. Three-tile pimples are the failure mode; bluffs and ridgelines are the success mode. Tuning happens at spec time against the existing genesis seed corpus.
- **No new naming.** The manual continues to use "cliff" where the steward bumps into one, because that is the word the steward will use. Internally, the system is _elevation + threshold_; the cliff is the noun applied at the moment of refusal.
- **Cave and ruin entrances on rises** (the v7-round arches) are still in scope but become a placement rule, not a tile-type interaction: the entrance is a normal `zoneTransition` tile placed somewhere along an unwalkable face. Defer the geometry to spec.

### Tracked as

- **Amendment to `RP-41`:** Cliffs are no longer an impassable tile type. The feature is implemented by raising the existing genesis elevation amplitude, gating movement on an `isClimbableStep` predicate, and accepting an unreachable cohort outside the spawn-connected `reachableMass`. `summary` and `notes` updated. `size: M`. Substrate reuse is high (existing `state.elevation`, existing iso lift); the spec work is tuning and the connectivity pass.
- **`RP-62 Reachable-mass-aware placement`** — S, depends on `['RP-41']`. Ruin placement (genesis and future RP-44 re-rolls), character spawning, and other guaranteed-reach systems read `state.reachableMass` as a precondition. Audit existing placement loops (`src/engine/genesis/`, `src/engine/characters.ts`) and route through the new precondition.
- **Amendment to `RP-42`:** Cliffs-as-firebreaks remains valid under the new framing — fire-spread BFS already adjacency-walks tiles; gating it on the same `isClimbableStep` predicate (or a fire-specific delta threshold) keeps the firebreak behavior without referencing a cliff tile type.
- **Amendment to `RP-44`:** Winter geology that lifts or drops elevation must recompute `state.reachableMass`. The unreachable cohort can grow and shrink across years — what was an isolated mesa one year may become reachable the next if the egregores raise a saddle.

### Open questions deferred to specs

- The exact threshold value, and whether it is constant or varies by terrain (a tile of bare earth might take a larger step than a tile of dense flora). (Boon)
- Whether the post-genesis connectivity pass guarantees a _minimum_ reachable mass size (refuse the seed and re-roll if too much of the map is islanded), or whether the seed is honored as-is and the steward gets whatever prairie the genesis hand delivered. (Boon, Astrid — Astrid's lean is the latter; the cosmology does not negotiate with the seed.)
- The cliff-face shadow's exact register — alpha, color, dither — and whether it reads cleanly at every iso angle. (Calla)
- The x-ray alpha rule's exact threshold and whether it applies only to terrain or also to flora/entities on lifted tiles. (Calla, Boon)
- Whether minor procedural micro-cliffs need explicit suppression in the noise (no rises shorter than N tiles long), or whether amplitude tuning is enough on its own. (Calla)
- Cave/ruin entrance geometry on cliff faces — the v7 "arch" idea — defers to spec entirely. (Boon)

---

## Round 4: the prairie's own evidence

_Tyler: "let's actually change the way reviewing camera film functions. right now it is stubbed in with incomplete behavior. brainstorm ways we can make this make sense with an ascii game. i don't know what to do. i was thinking about having text summaries explaining the photo but that's kinda not fun / doesn't make sense. i was thinking of drawing ascii art of the actual plants? but then i get tired thinking about sourcing all those. what is the point of the camera? what does it help the player accomplish or decide?"_

> **Astrid:** The question Tyler added is the question the round needs to answer: what is the camera _for_? It is not a tool. It is not RPG inventory. It is the prairie's evidence of its own slowing — the thing that holds a fragment of the world while the steward is somewhere else. The five lines name this directly: _the lineage is the medium_, and the camera is one of the few objects in the prairie capable of producing lineage. A photograph in this cosmology is not a trophy and not a record-of-mine — it is a witness, a thing that says _this happened here, while you were not._ The gel is the wrong register for that work; it speaks the scan instrument's language, abstract and decoded, decoupled from the soil it came out of. The photograph wants to speak the prairie's own language: the same iso the steward already reads, the same glyphs, the same colors, only held still and held smaller, with the grain of time on it. And — this is the cosmology speaking — the camera does not notice _events_. The camera notices _change_. Pollination is a category the camera does not know; bloom is a category the camera does not know. What the camera knows is that the cells in its footprint were one configuration and are now another. A roll of film over a patch that did not change yields one frame, and that one frame is true: _nothing happened here, and the camera was watching._ A roll over a patch that bloomed and was visited and was eaten yields many, and the spacing between them is the duration of the prairie's attention. The contact sheet is the natural shape — the roll is a single artifact made of moments where the prairie was no longer what it had been. _A photograph in this prairie is the prairie's own evidence of itself, and the roll is the prairie's evidence of having been more than one thing._

> **Boon:** The substrate audit makes both the capture-model shift and the playback shift cheaper than they look. `captureCells` at `src/engine/timeLapse.ts:103` already snapshots the rendered `{ char, color }` of all nine cells of the 3x3 footprint, in the same register `renderer.ts` uses for the world (tile base + flora overlay + bee/monarch overlay; the comment at line 49 names what is intentionally elided — cursor, fog, seasonal wash, path). Those cells live on every `TimeLapseFrame`. The capture trigger flips: instead of `recordCameraSubjectEvent` being called from seven or eight site-specific hooks (pollination, rain front, bloom, ember, monarch, ghost passage, egregore advance, character approach), a single hook in the sim loop — same cadence as flora lifecycle advancement (`src/engine/flora/spread.ts`) — calls `captureIfChanged(state, camera, now)`. Compute the current 9-cell state, compare against the last frame on the roll, append iff any cell differs. The eight call sites delete. The `subject: CameraSubject` field on `TimeLapseFrame` retires; the `subjectLabel` switch and the heading register in `TimeLapsePlayback.tsx:27-50` retire with it. (`CameraSubject` may live on if other systems use it — the scan instrument or a future spec — a small audit decides; if no one else uses it, the enum deletes too.) A baseline frame is captured at camera placement, so every subsequent diff has something real to diff against. Film count keys to captured changes one-to-one — one detected change consumes one film unit, which is the natural read and ties `RP-15` wear tighter than the event model ever did. Playback becomes a contact sheet: a CSS grid of N frames laid out chronologically, each rendered as a small iso-projected 9-cell photograph using the same iso transform `renderer.ts` uses, just at a reduced cell-pitch, framed with the existing `film-grain-overlay` class. Click-to-expand on a single frame is the held-read affordance — small modal over the contact sheet, cross-fade between adjacent frames if the steward steps; the contact sheet itself does not animate. `GelBandView` exits `TimeLapsePlayback.tsx` and stays alive in the codebase for the scan instrument, where it belongs. The hardest sub-decision is the diff predicate — strict any-cell-differs is the lean; a small N-tick stability filter prevents a bee crossing the footprint from generating a flurry of near-identical frames. Defer the exact heuristic to spec. Net code surface: smaller after than before. _The cheapest spec is the one that retires fields and call sites instead of populating them._

> **Calla:** The session-shape this unlocks is the one the camera was always reaching for. The steward picks up the camera, opens the roll, and sees the contact sheet — every moment the patch noticed change, laid out at once. A dead-soil camera with one frame says one thing: _I watched, and nothing happened._ A bloom-monarch-death camera with twelve frames says another: _look what you missed._ Both are legible at a glance, and both are honest in a way the old flipbook was not — the steward was being shown a sequence as if each frame was equally weighted, when in fact some patches had a lot to say and others did not. The contact sheet lets the prairie's variance be visible. Reading the sheet is the contemplative act: your eye traces the chronology, finds the frame where the clover became decay, sits with it. If a single frame wants a held read, click expands it; the rest of the sheet stays peripherally visible behind. The vocabulary is the same the steward already reads in the world, only held still — same iso, same glyphs, same colors, the only addition is the grain of time on the predecessor frames. Predecessor footage from `RP-24` works _better_ in this model than in the flipbook — the steward will encounter cameras they did not place, and the sparseness or density of the contact sheet tells them, before they read a single frame, what kind of place that was and how loudly its tenure rang. _The vocabulary is the same; only the tense is different — and now the rhythm is too._

> **Delta:** The frame to break is "the camera records events." The camera does not know what an event is. The camera knows what change is. Drop the subject taxonomy from the camera's mind entirely — `CameraSubject`, `recordCameraSubjectEvent`, the seven or eight site-specific hooks that were the camera trying to be a journalist, narrating what kind of thing it just saw. The camera is not a journalist; the camera is a witness, and a witness does not categorize. The witness notices that the world is no longer what it was, and records that. That is the whole job. The contact sheet is the natural shape because the roll is one artifact, not a sequence — you do not flip through someone's diary one page at a time. You spread it on the table and read it as a whole, and the moments you linger on are the moments _you_ choose. _The camera is patience, and patience is what you read in the spacing between frames._ One frame is a patient soil; twelve frames is a soil that had a year. The prairie's variance becomes a register the steward learns to read. Two practical fallouts. First — the camera captures a baseline frame the moment it is placed, so every subsequent frame is a diff against something real. The first frame is the camera saying _here is what was here when I started looking_. Second — the camera stays silent, as the consensus below already locks. No projector hum, no shutter click, no celebratory chime when a frame is recorded. The prairie does not announce itself; the camera does not either. _A photograph here is the prairie remembering on the steward's behalf, and the roll is the prairie's whole memory of one patch over one tenure._

### Consensus

- **The camera's purpose, named.** The camera is the prairie's evidence of moments the steward missed. It witnesses on behalf of the absent observer. Reviewing the roll is the steward reading what the prairie noticed while they were elsewhere.
- **The camera records change, not events.** Capture is diff-driven: a single hook in the sim loop computes the current 9-cell state, compares against the last frame on the roll, and appends a new frame iff any cell differs. `recordCameraSubjectEvent` and its seven or eight call sites retire.
- **The `CameraSubject` enum and the `subject` field on `TimeLapseFrame` retire from the camera pipeline.** `subjectLabel` and the heading switch in `TimeLapsePlayback.tsx:27-50` retire with them. If no other system uses `CameraSubject`, the enum deletes; audit at spec time.
- **A baseline frame is captured at camera placement.** Every subsequent frame is a diff against something real. The first frame says _here is what was here when I started looking._
- **Capture cadence matches flora lifecycle advancement.** One sim-loop hook, same heartbeat the prairie already advances on.
- **The photograph is the captured 3x3, iso-projected.** Playback renders the nine `TimeLapseCell` values using the same iso transform `renderer.ts` uses for the world, at a smaller cell-pitch, framed with the existing `film-grain-overlay` class.
- **The roll is a contact sheet.** Playback is a grid of all frames at once, chronological left-to-right, top-to-bottom, with timestamps under each. Variance is itself the read — a dead-patch roll shows one photo; a lively-patch roll shows many.
- **Click-to-expand on a single frame is the held-read affordance.** Stepping between adjacent frames in the expanded view cross-fades. The contact-sheet view itself does not animate.
- **The gel-band view is removed from primary playback.** `GelBandView` stays in the codebase for the scan instrument's use, where it belongs.
- **The camera stays silent.** No instrument voice added to review.
- **Annotation is rejected for this round.** No species names, no "what's here" overlay. The photographs speak.
- **Predecessor footage from `RP-24` shares this pipeline.** Degradation is a `degradation` prop (`{ grain, tint, glyphLeak }`) on the photograph component, applied per-frame in the contact sheet. Density of the sheet itself signals tenure liveliness before the steward reads any single frame.
- **3x3 footprint stays canonical.** Capture payload is unchanged; only the trigger model and the playback shape change.
- **Film count = captured changes.** One detected change consumes one film unit. Quiet patches preserve film; lively patches exhaust it. Tightens the loop with `RP-15` wear.

### Tracked as

- **Amendment to `RP-23`:** the camera's capture model and playback are redesigned per Round 4. Capture is diff-driven (change-detected at flora-lifecycle cadence) rather than event-driven; `recordCameraSubjectEvent`, its seven or eight call sites, the `CameraSubject` use in the camera, and the `subject` field on `TimeLapseFrame` retire. A baseline frame is captured at placement. Playback becomes a contact sheet — all frames at once, iso-projected, in a chronological grid — with an optional click-to-expand single-frame view (cross-fade between adjacent frames in expanded view only). `GelBandView` exits `TimeLapsePlayback.tsx` and is preserved for the scan instrument. The modal stays silent. Film count keys to captured changes one-to-one, sharpening the `RP-15` wear coupling. Implementation routes through `/change-request` against the existing RP-23 spec.
- **Amendment to `RP-24`:** predecessor footage rides the redesigned `RP-23` pipeline. Procedural generation produces a sequence of 9-cell `TimeLapseFrame` payloads in the same shape and renders into the same contact sheet. Degradation is a `degradation` prop (`{ grain, tint, glyphLeak }`) on the photograph component; current-tenure frames pass zero, predecessor frames pass non-zero. The 30% / 10% egregoric content bias from v4 R8 is unchanged; it is now expressed through `glyphLeak`. The contact-sheet density itself becomes a register the steward reads when encountering a predecessor's camera.

### Open questions deferred to specs

- The diff predicate. Strict any-cell-differs is the lean; a small N-tick stability filter prevents transient overlays (a bee crossing the footprint, a ghost passing through) from generating a flurry of near-identical frames. Default lean: stable-for-N-ticks with small N. (Boon)
- Capture cadence — flora-lifecycle heartbeat is the lean; spec confirms whether that is per-tick or per-game-day. (Boon)
- Contact sheet layout details — grid columns, frame size at small scale, timestamp formatting, behavior when the roll is very long (cap, scroll, wrap). (Boon, Calla)
- Click-to-expand behavior — does it allow stepping to adjacent frames with cross-fade (the round's lean), or is it strictly single-frame-with-close? (Calla, Delta)
- `CameraSubject`'s fate outside the camera — is the enum used by the scan instrument or any other system? Spec audit before deletion. (Boon)
- The seeded predecessor camera from `seedTenureStartFieldCamera` (`src/engine/timeLapse.ts:207`) currently authors its four seasonal frames as explicit `CameraSubject.SeasonalLandmark` payloads. The four frames are still valid as contact-sheet entries; only the metadata field changes. Spec confirms the migration shape. (Boon)
- Voynich-glyph leak in predecessor footage — does PUA glyph substitution layer in via the `degradation` prop, or is it an `RP-24` spec concern only? (Boon)

---

## Round 5: the prairie has gravity now

_Tyler: "it is a system and it needs to exist logically."_

> **Astrid:** Then the cosmology has already named what this is. Wildfire (`RP-42`) and winter geology (`RP-44`) are the two slow-bodied motions the steward cannot direct — heat death in lightning's brief form, egregoric reshaping in winter's long form. Waterfalls join them as the third, and they are older than either. Gravity is the oldest verb the prairie knows. Water moving downward is not a flourish on top of cliffs; it is what cliffs are _for_, cosmologically. The "logical" part Tyler is asking for is the part the cosmology insists on: a waterfall that exists without a source is a magic trick, and the prairie does not do magic tricks. Every waterfall must trace to a river or a pond above it; every drop must go somewhere below. The minute the prairie lets a waterfall appear floating on a cliff face with no source, it has betrayed itself. _Water moves because the prairie is being honest about elevation._ The doctrine lock: waterfalls are revealed, not added. They were always implied by RP-41; this round is the round where elevation and water finally speak to each other.

> **Boon:** Substrate audit. `state.rivers: Set<string>`, `state.ponds: Set<string>`, `state.tileWater: Map<string, number>`, and `state.elevation: Map<string, number>` already exist. RP-41 just shipped `state.reachableMass` and `isClimbableStep`. A waterfall is the intersection: any river or pond tile that has an unclimbable step to a lower walkable neighbor. Detection runs once post-genesis after rivers, ponds, and elevation are all settled — single linear pass over the union of river ∪ pond tiles, checking the four cardinal neighbors via `isClimbableStep`. New state: `state.waterfalls` keyed on the upper-edge tile (the water tile that drops), registered in `EXPECTED_FIELDS`. Per-waterfall ownership of (top, bottom) coordinates lives in a sibling structure if richer data is needed — defer the exact shape to spec. Render side: the waterfall draws on the iso side wall — the same surface RP-41's cliff shadow uses (`src/engine/tileBgCache.ts`, `paintTileBg`). Extension to that pass, not a new render slot. Per-frame cost: the set of waterfalls is fixed for a tenure (recomputed only at genesis and at RP-44 winter-geology boundaries and now at season boundaries for freeze/thaw), so the only per-frame work is iterating visible waterfalls and animating their glyph cycle — bounded by viewport size. Receiving tile gets a small `tileWater` bump so flora downstream of a waterfall has access to the moisture the fall delivers; full hydrology propagation is deferred. Net cost: one field, one detection pass, one render extension, one audio bridge, three recompute hooks (genesis, season boundary, RP-44 year boundary). Medium-to-large.

> **Calla:** Two things load-bear here, and the cosmology gets one of them right by accident. First: audio. The first time the steward walks past a waterfall in a tenure it should be _heard before it is seen_ — that is the moment the prairie reveals it has been hiding a feature in plain sight. Positional audio is the right register, not an ambient bed; the steward needs the spatial signal. Cap the concurrent audible count (Boon's perf worry doubles as a tonal worry — five waterfalls all audible at once is noise, not signal). Second, and this is where the cosmology gets a real gift: **the frozen waterfall is a stairway.** When winter freezes a waterfall, the flowing visual gives way to a frozen cascade — and the steward can climb it. A previously unreachable mesa above a waterfall becomes reachable in winter only. The prairie that refused the steward in summer welcomes them in winter, and the route closes again in spring. That is the kind of seasonal commitment the game has been waiting to give the steward — _go now or wait a year_. The thermogenic egregore proximity (`RP-43`) gives it a counterpoint: an egregore close enough to a frozen waterfall thaws it locally, and the stairway dissolves under the steward's feet. The cosmology bites back when you take what the egregores were sitting near. _What is given in winter can be taken by the warmth that has no right to be there._

> **Delta:** The line to lock is **the prairie has gravity now**. Cliffs gave the prairie _shape_; waterfalls give it _motion_. The frame to break is "we are adding waterfalls" — wrong verb. We are not adding anything. The water was already in `state.rivers` and `state.ponds`; the elevation was already in `state.elevation`; RP-41 made elevation matter for movement; this round is what's revealed when those two facts finally have a conversation. Astrid's no-floating-waterfalls lock and Calla's frozen-stairway move are the same idea seen from two angles: the system has to be honest, and the honesty produces a mechanic the cosmology can carry. The third leg of the motion tripod is in place — fire is the lightning-fast one, geology is the year-slow one, water is the one that has been here the whole time, hidden by the prairie pretending elevation was cosmetic. _The cosmology has gravity now. It always did. We just couldn't see it._ The room's lean on the open spec questions: yes the receiving tile gets a `tileWater` bump (small, decaying with seasonal phase if Boon wants the bookkeeping); yes a frozen waterfall is climbable upward (mesa-access is the whole reason this round earns its keep); yes a waterfall is a firebreak when RP-42 lands (it is moving water — fire does not cross moving water); yes a frozen waterfall affects `reachableMass` for the duration of winter only — and that recompute hook is the contract RP-44 needs to honor anyway. The cosmology has been waiting for this round since v7 named verticality. _Gravity was always older than fire._

### Consensus

- **Waterfalls are a system, not decoration.** Every waterfall must trace to a river or pond above it; standalone waterfalls forbidden. _Water moves because the prairie is being honest about elevation._
- **Detection.** Post-genesis pass over the union of `state.rivers ∪ state.ponds`. For each water tile, check the four cardinal neighbors via `isClimbableStep` (RP-41); if the neighbor is walkable but the step is unclimbable downward, record a waterfall. Runs once at genesis; recomputed when RP-44 winter geology mutates elevation, and at season boundaries for freeze/thaw.
- **New GameState field: `state.waterfalls`.** Set or sibling Map keyed on the upper-edge tile. Single-owner write (genesis + RP-44 hook + season boundary hook). Registered in `EXPECTED_FIELDS`. Exact shape deferred to spec.
- **Receiving tile bump.** The lower tile that the water falls onto gets a `tileWater` increment so downstream flora has access to the moisture. Full hydrology watershed propagation deferred — the bump is local, not transitive.
- **Render.** Animated glyph cycle on the iso side wall between top and bottom tiles. Extension to `tileBgCache.paintTileBg` (the same site RP-41 added the cliff shadow). No new render pass. Existing water palette; no rainbows, no new glyph families. Exact glyph rotation and cadence deferred to spec.
- **Audio: positional per-waterfall.** Distance-weighted volume; concurrent-audible cap to avoid the prairie sounding like a fountain mall. Asset choice deferred to spec.
- **Frozen waterfall is a stairway.** When `weather.season === Winter`, waterfall tiles become climbable upward via the frozen cascade — `isClimbableStep` returns true for the bottom→top transition on a frozen waterfall in winter. Mesas above waterfalls become reachable in winter only. Spring thaws the cascade and the route closes. _What is given in winter can be taken by the warmth that has no right to be there._
- **RP-43 thermogenic egregore counterpoint.** A frozen waterfall within the heat radius of a thermogenic egregore thaws locally during winter — the stairway dissolves. The egregore can revoke a route the prairie offered.
- **Firebreak.** Waterfalls count as firebreaks once RP-42 (live wildfire) lands. Fire-spread BFS rejects waterfall tiles for the same reason it rejects ordinary water tiles — moving water does not burn.
- **`reachableMass` participation.** Frozen waterfalls extend `state.reachableMass` for the duration of winter. RP-44 must recompute `reachableMass` at season boundaries when waterfalls freeze/thaw, not only at year boundaries. **Amendment to v11 R3 `reachableMass` lock:** the set is no longer purely genesis-time + RP-44 year-boundary; it has a seasonal recompute hook tied to frozen waterfalls.
- **Cosmology lock.** Waterfalls join wildfire (`RP-42`) and winter geology (`RP-44`) as the three motion-the-steward-cannot-direct systems. The line: **the prairie has gravity now.**

### Tracked as

- **Amendment to `RP-64`** (Waterfalls). Scope locked. Mechanic, system shape, and seasonal interactions all named. Size L (the frozen-stairway mechanic, `reachableMass` recompute hook, and audio cap meaningfully grow the scope from the original placeholder M). `depends_on` expanded to `['RP-41', 'RP-43']`; RP-42 firebreak interaction is documented but not a hard dependency since fire BFS will read `state.waterfalls` opportunistically when it lands. `notes` updated with the round summary.
- **Amendment to `RP-44`:** the winter-geology spec must include a `reachableMass` recompute trigger at season boundaries (Winter→Spring thaw, Autumn→Winter freeze) in addition to the year-boundary recompute originally specced — frozen waterfalls change reachability in-tenure.
- **Amendment to v11 R3 lock on `reachableMass`:** the field is no longer purely genesis-time + RP-44 year-boundary; it has a seasonal recompute hook from RP-64 frozen waterfalls.

### Open questions deferred to specs

- Exact `state.waterfalls` shape — `Set<string>` keyed on top tile vs `Map<string, { topX, topY, bottomX, bottomY, frozen }>`. Lean: Map (Calla wants the frozen flag accessible without a lookup; Boon wants the shape to support sibling systems like audio without extra joins).
- Exact glyph rotation for flowing vs frozen states. (Astrid, Calla) — no Voynich, no rainbow; defer to spec.
- Audio asset: reuse existing water sample with a low-pass and reverb, or commission a new sample. (Calla)
- Concurrent-audible cap value, and whether the cap is hard (drop excess) or soft (weighted attenuation). (Boon, Calla)
- Whether the receiving tile's `tileWater` bump decays with seasonal phase or persists. (Boon)
- Multi-tile cliff face — when a tile drops two or more elevation deltas to its neighbor in one step, is that one waterfall or staged waterfalls along the way down? (Boon) Lean: one waterfall, scaled visually by the lift difference.
- Whether the frozen stairway is walkable downward as well as upward. (Calla) Lean: upward only.
- Whether a frozen waterfall whose source dries up (river rerouted by RP-44 geology) immediately falls apart, or persists as an ice cascade for the rest of winter. (Astrid — _the prairie does not maintain dishonesty_; lean: falls apart immediately).
- Whether the visual register for a waterfall extends to anything ABOVE the cliff (a "rapids" effect on the source tile as the water moves toward the edge), or stays strictly on the side wall. (Calla) Lean: side wall only.

---

## Round 6: a typewriter is a save point only where there is dying

_Tyler: "I like the idea of a typewriter existing in the little house. In the Resident Evil games I always found it quite evocative that you saved your game with it. I don't want free-form letters — if anything it'd need to be like FromSoft, where you build messages from templates. But I don't know if this is a feature we need yet. Seems like very later MMO stuff."_

> **Astrid:** The Resident Evil typewriter saves because Jill is dying. The ribbon is a stay of execution, not a literary act. _The prairie has no stay of execution._ The tenure ends at the snow line whether you typed or not, whether you remembered or not, and the cosmology is unmoved by the steward's eloquence. A typewriter that saves the game would be a heresy against the second line — _tending is the verb, not preserving_. A typewriter that builds messages from templates the way a FromSoft sign does would be a heresy against the prairie's solitude — there are no other stewards in this tenure to read what you carve. I will not say no to the object. I will say no to giving it a verb that the prairie does not already own.

> **Boon:** Then the proposal is furniture, and furniture doesn't earn a system. Zero LOC outside the render path, no `state.pages`, no `consumables.ribbon`, no `consumables.paper`, no new field in `EXPECTED_FIELDS`. If we tracked every evocative interior object as a backlog item we'd have forty entries for chairs. _The cost of an unused feature is not zero — it's the slot it takes from a feature that pays rent._ The honest path is: the typewriter sits on the desk, the player walks past it, and the day we know what it does we open a round and spec it then. The thing worth tracking is not the typewriter — it is _the question of whether the cellar Knot eventually needs a diegetic surface_, with the typewriter as the leading candidate object.

> **Calla:** I want to push back on _just decor_, though. A typewriter in the room with paper next to it and a ribbon spool that the camera light catches is a Chekhov problem. The first session a new steward sees it, they will try to interact with it. If nothing happens — not even a small line of acknowledgement — we've trained them that objects in the little house are inert. That bleeds into the camera, the bed, the cellar door, every other diegetic surface. The little house's promise so far is _everything you can see, you can touch_. If the typewriter ships before it does anything, it has to either say "not yet" out loud — a tiny prairie-voiced line, no fourth wall break — or it stays in a drawer until it earns the desktop.

> **Delta:** The camera is already the save point. _The shutter is the ribbon striking the platen._ Both are slow, costly, deliberate acts where the steward stops moving and commits a specific moment to the lineage; both consume a finite resource (film, ribbon); both produce an artifact that outlives the tenure. Tyler is reaching for a feeling that's already in the game, and asking whether we should build a second instrument that does the same thing. I think the question Round 6 is actually asking is: _does the cellar Knot eventually need a physical interface_ — a thing the steward walks up to in the little house and presses a key on, instead of the Knot being an abstract deposit that happens at season-end? If the answer is yes, the typewriter is the candidate object. If the answer is no, the typewriter is a chair. Track the question. The typewriter rides along with it.

### Consensus

- **The typewriter is tracked as its own item, scoped as decor-pending-a-problem.** It is not amended onto `RP-33` (already shipped). It is a standalone backlog entry so the round's reasoning persists and the room can come back to it when a problem demands it.
- **No `state.pages`, no consumables, no new fields in v11.** The room is unanimous: nothing in `GameState` shifts for this round.
- **No FromSoft-style template messaging.** Astrid's line holds — the prairie is solitary within a tenure. Cross-tenure speech already has a channel (the cellar Knot, the lineage); a second channel needs a real reason.
- **No save-point semantics.** Saves are a Resident Evil affordance against permadeath; the prairie's tenure-as-unit lock makes "save" a category error.
- **Chekhov mitigation: the typewriter shows a small prairie-voiced refusal on interaction.** A single line — copy deferred — said once per tenure, then silent. Acknowledges presence without promising a system. Calla's responsiveness rule is preserved without committing to a feature.
- **The leading hypothesis for what the typewriter eventually becomes** is the diegetic interface to the cellar Knot — the physical surface where a steward composes a Knot deposit. Tracked as an open question, not a decision.

### Tracked as

- **`RP-66` Typewriter, paper, and ink ribbon (decor-pending-a-problem).** Size XS. `depends_on: []`. Static decor placed on the desk in the little house with a one-time prairie-voiced refusal line on interaction. No state changes, no consumables, no field additions, no new verbs. The item exists so the round 6 reasoning — _why this is not a system yet, what it might become_ — persists in the backlog instead of evaporating.

### Open questions deferred to specs

- Does the cellar Knot eventually want a diegetic interface in the little house, and if so is the typewriter that interface? (Delta — the reframe; Astrid would want any such interface to be a ritual, not a menu.)
- The exact wording of the typewriter refusal line. (Calla — must not break the fourth wall; must not promise a future feature; must not sound like a developer comment.)
- Whether the ribbon spool's color (a small palette choice) changes by tenure-year as a quiet lineage marker, or stays fixed. (Boon — defer; only matters if the typewriter ever becomes interactive.)
