// RP-37 — Knot Cellar knot-glyph render pass.
//
// Iterates state.archivedKnots and stamps a `§` glyph at each alcove
// position. Renders one extra `§` at alcove index archivedKnots.length
// while state.bedKnotPresent is true (the in-hand knot whose archive
// entry has not yet been committed at Winter→Spring per RP-36).
//
// Stub created during /new-feature spec authoring.
export {}
