// RP-22 — Chronicle template registry.
//
// CHRONICLE_TEMPLATES is the single source of truth for the past-tense
// sentences chronicle events render through. The registry maintains a
// ≥50% negative-tone ratio across the whole file; a unit test
// enforces this.
//
// Implementation follows in the plan tasks.

export {}
