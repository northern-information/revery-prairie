// RP-22 — Chronicle emitters.
//
// World-state-transition observers that call addChronicleEvent. Each
// emitter takes (state, ...transition-context) and returns void. No
// player-action source file imports from this module — the contract is
// enforced by an architecture test.
//
// Implementation follows in the plan tasks.

export {}
