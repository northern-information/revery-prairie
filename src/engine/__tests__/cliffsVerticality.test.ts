// Stub test file for RP-41 — populated by the plan's implementation tasks.
// See harness/specs/RP-41-cliffs-verticality.yaml verification block.
import { describe, it } from 'vitest'

describe('cliffs and verticality', () => {
  it.todo('isClimbableStep returns false when delta exceeds threshold')
})
