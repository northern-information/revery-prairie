// Stub test file for RP-65 — populated by the plan's implementation tasks.
import { describe, it } from 'vitest'

describe('tile x-ray', () => {
  it.todo('detects lifted tiles occluding the avatar')
})
