// Spec acceptance test stub for precis-4-the-revery.
// Detailed unit tests will live alongside the implementation modules
// (revery.test.ts, etc.). This file is the spec's acceptance surface and is
// filled in by the final plan task.
import { describe, it } from 'vitest'

describe('the revery (precis #4)', () => {
  it.todo('revery-state-on-gamestate: GameState exposes revery, reveryCount, cosmologicalDrift')
  it.todo('omen-detection: any of three world events schedules the Revery when prerequisites pass')
  it.todo('revery-enter-and-observe: Observing phase locks input and runs time-compressed winter')
  it.todo('revery-input-lock: isReveryLocked blocks movement and keyboard handlers')
  it.todo('revery-resolves-phenotype-label: most-discovered species gains a hedged label')
  it.todo('revery-first-egregore-advance: only the first Revery places 3 new egregore tiles')
  it.todo('revery-summary-phase: end-of-Revery summary renders ASCII + Voynich lines')
  it.todo('revery-closing-and-reset: closing increments reveryCount and clears state.revery')
  it.todo('manual-renders-revealed-phenotypes: phenotype labels appear in ManualPanel below lore')
})
