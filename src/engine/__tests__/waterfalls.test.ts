// Stub test file for RP-64 — populated by the plan's implementation tasks.
// See harness/specs/RP-64-waterfalls.yaml verification block.
import { describe, it } from 'vitest'

describe('waterfalls', () => {
  it.todo('detectWaterfalls returns entries for unclimbable water-tile edges')
})
