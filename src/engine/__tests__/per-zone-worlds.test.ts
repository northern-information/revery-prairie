// Placeholder. Tests are added in plan task `add-per-zone-worlds-tests`
// (see harness/plans/per-zone-ecs-worlds.yaml).
import { describe, expect, it } from 'vitest'

describe('per-zone-worlds (placeholder)', () => {
  it('placeholder until plan task lands real tests', () => {
    expect(true).toBe(true)
  })
})
