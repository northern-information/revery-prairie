// Precis #4 — The Revery summary overlay. Implementation lands via the plan.
// See harness/specs/precis-4-the-revery.yaml for the locked rendering rules.
//
// Renders bilingual ASCII + Voynich change log when state.revery.summaryReady
// is true. Dismissed by keypress or click; transitions revery.phase to Closing.

export {}
