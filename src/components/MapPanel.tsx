import type { GameState } from '@/engine/types'

// RP-70 — The Map permacomputer tab. Read-only ASCII chart of the
// prairie rendered on its own canvas. Implementation follows the
// approved plan; this is the initial scaffold.
interface MapPanelProps {
  state: GameState
}

export const MapPanel = (_props: MapPanelProps) => {
  return null
}
