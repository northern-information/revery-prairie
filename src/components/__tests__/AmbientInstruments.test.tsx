// Placeholder for precis-28 — AmbientInstruments test suite will be implemented
// by the harness pipeline. See harness/specs/precis-28-permacomputer-instruments.yaml.
import { describe, it } from 'vitest'

describe('AmbientInstruments', () => {
  it.todo('renders the weathervane with rotated cardinal icon and letter')
  it.todo('renders the wind-speed label from smoothSpeed via even fifths')
  it.todo('renders the sky icon for state.weather.sky')
  it.todo('renders the season label in Title Case')
  it.todo('renders the almanac line with percentage and next bookmark')
  it.todo('is a pure render — no hooks, no state writes')
})
